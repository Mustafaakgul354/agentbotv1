# AgentBot Environment Variables
# Copy this file to .env and fill in your values

# Session Store Encryption (Recommended)
# Generate a key with: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
AGENTBOT_SESSION_KEY=

# Message Bus Backend (memory or redis)
# Default: memory
# AGENTBOT_BUS=memory
# AGENTBOT_BUS=redis

# Redis URL (only needed if AGENTBOT_BUS=redis)
# REDIS_URL=redis://localhost:6379/0

# Config Path (usually set in docker-compose.yml, override if needed)
# AGENTBOT_CONFIG=/app/config/runtime.yml

# LLM Configuration (Optional)
# Enable OpenAI LLM support
# AGENTBOT_LLM=openai
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4o-mini

# BrowserQL Configuration (Optional)
# Use BrowserQL instead of Playwright for browser automation
# BROWSERQL_TOKEN=your-browserless-token

# Audit Log Path (Optional)
# Default: artifacts/audit.log
# AGENTBOT_AUDIT_LOG=artifacts/audit.log

# Artifacts Directory (Optional)
# Default: artifacts
# AGENTBOT_ARTIFACTS=artifacts
